import { EventEmitter } from 'events';
import { ConsensusConfig, ConsensusResult } from './types';
export declare class RAFTConsensus extends EventEmitter {
    private logger;
    private config;
    private state;
    private currentTerm;
    private votedFor;
    private log;
    private commitIndex;
    private lastApplied;
    private nextIndex;
    private matchIndex;
    private electionTimer?;
    private heartbeatTimer?;
    private batchBuffer;
    private pipelineDepth;
    constructor(config: ConsensusConfig);
    initialize(): Promise<void>;
    start(): Promise<void>;
    stop(): Promise<void>;
    private startElectionTimer;
    private startElection;
    private requestVotes;
    private becomeLeader;
    private startHeartbeat;
    private sendHeartbeats;
    proposeBlock(block: any): Promise<ConsensusResult>;
    private replicateEntry;
    private applyEntry;
    processBatch(transactions: any[]): Promise<ConsensusResult>;
    private generateBatchId;
    requestJoinValidatorSet(request: any): Promise<void>;
    requestLeaveValidatorSet(nodeId: string): Promise<void>;
    private reconfigureCluster;
    private sendVoteRequest;
    private sendAppendEntries;
    private forwardToLeader;
    getHealth(): Promise<any>;
}
//# sourceMappingURL=RAFTConsensus.d.ts.map